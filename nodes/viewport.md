# Viewport (Node)

!!!warning
A viewport is not currently implemented as a Node
!!!

A viewport provides a render target that can be drawn onto by other nodes. Paired with a [Camera](camera.md), it can supply a view into a [Stage](../stages.md).
